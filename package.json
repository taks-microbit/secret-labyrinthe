{
  "name": "secret-labyrinthe",
  "version": "0.1.0",
  "description": "Un super jeu avec des minotaures, des dragons, des labyrinthes...",
  "scripts": {
    "prepare": "rm -rf ./dist && cp -r ./src/public ./dist",
    "format": "prettier -w src",
    "bundle": "webpack",
    "build": "npm run format && npm run prepare && npm run bundle"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/jquery": "^3.5.14",
    "prettier": "^2.7.1",
    "ts-loader": "^9.3.1",
    "typescript": "^4.7.4",
    "webpack": "^5.73.0",
    "webpack-cli": "^4.10.0"
  },
  "dependencies": {
    "jquery": "^3.6.0"
  }
}
